extends layout

block title
    | Register

block content

    form
        h2(class="block-panel-heading") Register new account
        input(
            type="text"
            class="form-control credentials"
            placeholder="Email address"
            required=""
            autofocus=""
        )
        input(
            type="password"
            class="form-control credentials"
            placeholder="Password"
            required=""
        )
        button(
            class="btn btn-lg btn-primary btn-block"
            type="submit"
        ) Register
